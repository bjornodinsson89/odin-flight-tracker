# odin-flight-tracker

## v1.0.7

- Clears tracked state immediately when tracking mode or manual target changes.
- Auto enemy tracking now resolves ranked-war opponents from faction war API data before DOM fallback.
- Stale auto-enemy entries are pruned when opponents change or disappear.
